$(function() {

	var headerH = $("#header"),
	    blockH = $("#block").innerHeight(),
	    scrollOffset = 0;	

	$(window).on("scroll", function() {
	
		scrollOffset = $(this).scrollTop();
		
		if(scrollOffset >= blockH) {
			header.addClass
		}
		console.log(scrollOffset);
	});


});